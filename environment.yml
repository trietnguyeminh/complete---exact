name: complete-exact
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - scikit-learn
  - tqdm
  - jupyter
  - ipykernel
  - pip:
      - z3-solver>=4.12
      - torch>=2.1
      - transformers>=4.41
      - datasets>=2.19
      - accelerate>=0.30
      - peft>=0.11
      - trl>=0.9
      - bitsandbytes>=0.43
      # Optional GPU packages:
      # - vllm>=0.5
      # - unsloth
      # - xformers
