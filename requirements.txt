# Core utilities for archive validation and local reports
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
tqdm>=4.66

# Symbolic reasoning
z3-solver>=4.12

# Notebook support
jupyter>=1.0
ipykernel>=6.0

# LLM / fine-tuning / inference stack used in the project
# GPU/CUDA environment may be required for these packages.
torch>=2.1
transformers>=4.41
datasets>=2.19
accelerate>=0.30
peft>=0.11
trl>=0.9
bitsandbytes>=0.43

# Optional GPU inference stack:
# vllm>=0.5
# unsloth
# xformers
